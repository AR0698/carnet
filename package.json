{
  "name": "carnet",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run pack && tsc && vitest run && vite build",
    "pack": "node scripts/build-pack.mjs english-grammar && node scripts/build-pack.mjs vocabulary && node scripts/build-pack.mjs bristol-culture",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "icons": "node scripts/make-icons.mjs"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "fake-indexeddb": "^6.2.5",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource/anton": "^5.3.0",
    "@fontsource/permanent-marker": "^5.3.0",
    "dexie": "^4.4.4",
    "ts-fsrs": "^5.4.1"
  }
}
